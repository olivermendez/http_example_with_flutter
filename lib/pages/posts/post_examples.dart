import 'package:flutter/material.dart';

class PostExample extends StatelessWidget {
  const PostExample({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("With post now"),
    );
  }
}
